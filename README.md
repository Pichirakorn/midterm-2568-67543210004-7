# Library Management System - Layered Architecture

## 📋 Project Information
- **Student Name:** พิชิรกร ชาติปิระ
- **Student ID:** 67543210004-7
- **Course:** ENGSE207 Software Architecture

## 🏗️ Architecture Style
Layered Architecture (3-tier)

## 📂 Project Structure
แยกเป็น 3 folder
- business : จัดการ business logic และ validation ของนะบบ
- data : จัดการการเข้าถึงฐานข้มูลและการทำงานกับ SQLite
- presentation : จัดการ HTTP request/response, routes, controllers และ error handling

## 🎯 Refactoring Summary

### ปัญหาของ Monolithic (เดิม):
- โค้ดทั้งหมดอยู่รวมกันในไฟล์หรือ layer เดียว ทำให้โค้ดอ่านยาก
- แก้ไขหรือเพิ่มฟีเจอร์ใหม่เสี่ยงกระทยส่วนอื่นของระบบ
- ทดสอบระบบได้ยาก เพราะแต่ละส่วนไม่แยกความรับผิดชอบ
- การดูแลรักษาทำได้ยากเมื่อระบบขยายใหญ่ขึ้น

### วิธีแก้ไขด้วย Layered Architecture:
- แยกระบบออกเป็น Presentation, Business และ Data Layer
- Presentation Layer ทำหน้าที่รับ–ส่งข้อมูลผ่าน API โดยไม่รู้รายละเอียดของ business logic
- ย้าย business rules และ validation ไปไว้ใน Business Layer

### ประโยชน์ที่ได้รับ:
- โค้ดเป็นระเบียบ อ่านง่าย และเข้าใจโครงสร้างระบบได้ชัดเจน
- แก้ไขหรือเพิ่มฟีเจอร์ใหม่ได้ง่าย โดยไม่กระทบระบบอื่น
- สามารถทดสอบแยกกันได้
- ระบบมีความยยืดหยุ่นรองรับการขยาตัว

## 🚀 How to Run

\`\`\`bash
# 1. Clone repository
git clone [Pichirakorn Chartpira 67543210004-7](https://github.com/Pichirakorn/engse207-midterm-individual)

# 2. Install dependencies
npm install

# 3. Run server
npm start

# 4. Test API
# Open browser: http://localhost:3000
\`\`\`

## 📝 API Endpoints
GET     `/api/books`             ดึงรายการหนังสือทั้งหมด
GET     `/api/books/:id`         ดึงข้อมูลหนังสือตาม ID
POST    `/api/books`             เพิ่มหนังสือใหม่
PUT     `/api/books/:id`         แก้ไขข้อมูลหนังสือ
PATCH   `/api/books/:id/borrow`  ยืมหนังสือ
PATCH   `/api/books/:id/return`  คืนหนังสือ
DELETE  `/api/books/:id`         ลบหนังสือ
<img width="156" height="411" alt="pichi drawio" src="https://github.com/user-attachments/assets/d3439d1b-2c8e-4b20-bccb-7f2630d3ee0f" />

สถาปัตยกรรมแบบ 3-tier Layered Architecture

Presentation Layer ทำหน้าที่จัดการคำร้องขอ, เส้นทางการทำงาน, ตัวควบคุมการทำงาน และการส่งผลลัพธ์กลับไปยังผู้ใช้งาน

Business Layer ทำหน้าที่ประมวลผลตรรกะทางธุรกิจ และตรวจสอบความถูกต้องของข้อมูล (Validation Rules)

Data Layer ทำหน้าที่จัดการการเข้าถึงฐานข้อมูลผ่าน Repository และระบบฐานข้อมูล SQLite

แต่ละ Layer มีความรับผิดชอบเฉพาะของตนเอง
